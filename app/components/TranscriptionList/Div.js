import styled from 'styled-components';

import Button from 'components/Button';

const Div = styled.div`
  position: relative;

  > div:nth-child(2) {
    width: 100%;
    text-align: center;
  }
`;

export default Div;
